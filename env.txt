DATABASE_URL="sqlite:///%kernel.project_dir%/var/data.db"

# Cette configuration utilise SQLite avec une base de données stockée dans var/data.db
# C'est parfait pour le développement et la production sur VPS 